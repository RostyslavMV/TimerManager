<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:TimerManager">
    <ResourceDictionary.MergedDictionaries>
        <!--Order is important-->
        <ResourceDictionary Source="Colors.xaml"/>
        <!--<ResourceDictionary Source="Fonts.xaml"/>-->
        <ResourceDictionary Source="Texts.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Timer Control Button -->
    <Style TargetType="{x:Type Button}" x:Key="TimerControlButton">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource LogoBlueBrush}" />
    </Style>

    <!-- Main Window Button -->
    <Style TargetType="{x:Type Button}" x:Key="MainWindowsButton">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BackgroundLightBrush}"/>
    </Style>

</ResourceDictionary>